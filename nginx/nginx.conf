events {
    worker_connections 1024;
}

http {
    include       /etc/nginx/mime.types;
    default_type  application/octet-stream;

    # Development: HTTP server (Port 80) - No SSL required
    server {
        listen 80;
        server_name localhost;

        # Serve React App
        root /usr/share/nginx/html;
        index index.html;

        location / {
            try_files $uri $uri/ /index.html;
        }

        # Proxy Socket.IO to backend
        location /socket.io/ {
            proxy_pass http://server:3000;
            proxy_http_version 1.1;
            
            # WebSocket upgrade headers
            proxy_set_header Upgrade $http_upgrade;
            proxy_set_header Connection "upgrade";
            
            # Standard proxy headers
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;
            proxy_set_header X-Forwarded-Host $host;
            proxy_set_header X-Forwarded-Port $server_port;
            
            # WebSocket timeouts (important for long-lived connections)
            proxy_read_timeout 86400;
            proxy_send_timeout 86400;
            
            # Disable buffering for WebSockets
            proxy_buffering off;
            proxy_cache off;
        }
    }

    # Production: HTTPS server (Port 443) - SSL required
    # Uncomment this block when SSL certificates are available
    # server {
    #     listen 443 ssl;
    #     server_name localhost;
    #
    #     # SSL certificates (required for HTTPS)
    #     ssl_certificate     /etc/nginx/certs/selfsigned.crt;
    #     ssl_certificate_key /etc/nginx/certs/selfsigned.key;
    #
    #     # Serve React App
    #     root /usr/share/nginx/html;
    #     index index.html;
    #
    #     location / {
    #         try_files $uri $uri/ /index.html;
    #     }
    #
    #     # Proxy Socket.IO to backend
    #     location /socket.io/ {
    #         proxy_pass http://server:3000;
    #         proxy_http_version 1.1;
    #         proxy_set_header Upgrade $http_upgrade;
    #         proxy_set_header Connection "upgrade";
    #         proxy_set_header Host $host;
    #         proxy_set_header X-Real-IP $remote_addr;
    #         proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    #         proxy_set_header X-Forwarded-Proto $scheme;
    #     }
    # }
}